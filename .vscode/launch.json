{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "node",
            "request": "launch",
            "name": "launch server.js",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "envFile": "${workspaceFolder}\\.env",
            "program": "${workspaceFolder}\\server.js"
        }
    ]
}